# Service: ClusterIP на поды backend, порт 8080.
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: todo-app
  labels:
    app: backend

spec:
  selector:
    app: backend
  ports:
    - port: 8080
      targetPort: http
